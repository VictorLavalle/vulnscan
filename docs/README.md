# Screenshots

Add screenshots here for the README:

- `vulnscan-summary.png` — The grouped table output (referenced in README)
- `checkscan.png` — Checkov compact output
- `vulnscan-table.png` — Standard grype table output

## How to take screenshots

```bash
# On macOS: Cmd+Shift+4 then select area
# Save directly to this folder

# Or use a terminal screenshot tool:
# brew install termshot
# termshot -f docs/vulnscan-summary.png -- vulnscan-summary
```
